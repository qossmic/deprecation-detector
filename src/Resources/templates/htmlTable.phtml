<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>SensioLabs DeprecationDetector</title>

    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css">
</head>
<body>
    <div class="container-fluid">
        <h1>SensioLabs DeprecationDetector <span class="project"></span> at <span class="date"><?php echo date('Y-m-d'); ?></span></h1>
        <p>Found <?php echo count($violations) ?> violations in <?php echo count($orderedViolations) ?> files.</p>
        <table class="table table-striped table-bordered table-hover">
            <thead>
                <tr>
                    <th>#</th>
                    <th>Usage</th>
                    <th>Line</th>
                    <th>Comment</th>
                </tr>
            </thead>
            <tbody>
                <?php $index = 1; ?>
                <?php foreach($orderedViolations as $file => $violations):?>
                <tr class="file info">
                    <td colspan="4" class="filename"><?php echo $file?></td>
                </tr>
                <?php foreach($violations as $violation): ?>
                <tr class="violation">
                    <td class="index"><?php echo $index++; ?></td>
                    <td class="usage"><?php echo strip_tags($violation['message']); ?></td>
                    <td class="line"><?php echo $violation['line'] ?></td>
                    <td class="comment"><?php echo $violation['comment'] ?></td>
                </tr>
                <?php endforeach; ?>
                <?php endforeach; ?>
            </tbody>
        </table>
        <p>Generated by SensioLabs DeprecationDetector using php in version <span class="php-version"><?php echo phpversion()?></span> at <span class="date"><?php echo date('Y-m-d H:i:s')?></span>.</p>
    </div>
</body>
</html>
